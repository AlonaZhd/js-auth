{{!
Це код container файлу для index сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/"
}}

<div class="page page--bg">
    <header>
        {{> back-button}}
    </header>

    <form class="page__section">
        <h1 class="page__title">Реєстрація</h1>

        <div class="form">
            <div class="form__item">
                {{> field action="singupForm.change" label='Ел. адреса' type='email' name='email'
                placeholder="Ваш.e-mail"}}
                <span name="email" class="form__error">Помилка</span>
            </div>

            <div class="form__item">
                {{> field-password action="singupForm.change" label='Пароль' type='password' name='password'
                placeholder="Введіть пароль"}}
                <span name="password" class="form__error">Помилка</span>
            </div>
        </div>

        <button onclick="singupForm.submit()" class="button" type="button">Зареєструватися</button>

        <span class="alert alert--error">Увага, помилка!</span>
    </form>
</div>

{{!
Цей код містить лише те, що буде виводитись в

<body>
    Адже наш layout/default.hbs має вже структуру HTML
    }}